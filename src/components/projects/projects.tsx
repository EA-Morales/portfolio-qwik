import { component$ } from "@builder.io/qwik";

export const Projects = component$(() => {
  return (
    <div class='min-h-screen' id='projects'>
      <h2 class='text-primary font-script mb-6 text-xl'>
        click on any project to learn more!
      </h2>
    </div>
  );
});
